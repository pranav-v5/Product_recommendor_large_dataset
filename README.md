# Product_recommendor_large_dataset
This is my first ai(llm) based small poc where i've a large datasets of all products(approx 1k datasets). Where a user query is gievna dn response is generated based on datasets.
